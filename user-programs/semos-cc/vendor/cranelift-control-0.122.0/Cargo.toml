# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

# D.2 port: each vendored crate is its own workspace root so cargo doesn't
# walk up to semos-cc/Cargo.toml looking for membership.
[workspace]

[package]
edition = "2024"
rust-version = "1.86.0"
name = "cranelift-control"
version = "0.122.0"
authors = ["The Cranelift Project Developers"]
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "White-box fuzz testing framework"
readme = "README.md"
keywords = [
    "fuzz",
    "test",
]
license = "Apache-2.0 WITH LLVM-exception"
repository = "https://github.com/bytecodealliance/wasmtime"
resolver = "2"

[features]
chaos = ["fuzz"]
# D.2 SemOS port: dropped `fuzz` from default to keep `arbitrary` out of
# the no_std build. arbitrary 1.4.2 uses std::error::Error ungated.
default = []
fuzz = ["dep:arbitrary"]

[lib]
name = "cranelift_control"
path = "src/lib.rs"

[dependencies.arbitrary]
version = "1.4.0"
optional = true
