# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

# M27 Phase 5c Stage G iter 5b: opt out of parent workspace discovery —
# this vendored crate lives under user-programs/semos-rustc/vendor-externals/,
# so cargo would otherwise try to find a [workspace] header above. The
# parent workspace explicitly only globs vendor-rustc-src/compiler/*.
[workspace]

[package]
edition = "2024"
rust-version = "1.86.0"
name = "cranelift-frontend"
version = "0.122.0"
authors = ["The Cranelift Project Developers"]
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Cranelift IR builder helper"
documentation = "https://docs.rs/cranelift-frontend"
readme = "README.md"
categories = ["no-std"]
license = "Apache-2.0 WITH LLVM-exception"
repository = "https://github.com/bytecodealliance/wasmtime"
resolver = "2"

[features]
core = [
    "hashbrown",
    "cranelift-codegen/core",
]
# M27 Phase 5c Stage G iter 5b: switch default from "std" to "core" so
# transitive consumers (rustc_codegen_cranelift) that pull this crate
# without `default-features = false` don't drag std into cranelift-codegen.
# The `core` feature enables the hashbrown HashMap path which is what
# x86_64-unknown-none needs.
default = ["core"]
std = ["cranelift-codegen/std"]

[lib]
name = "cranelift_frontend"
path = "src/lib.rs"

[dependencies.cranelift-codegen]
version = "0.122.0"
# M27 Phase 5c Stage G iter 5b: drop "std" — the consumer (rustc_codegen_
# cranelift) builds against x86_64-unknown-none; std is unavailable.
# unwind keeps gimli no_std-friendly DWARF tables.
features = [
    "unwind",
]
default-features = false

[dependencies.hashbrown]
version = "0.15"
optional = true
default-features = false

[dependencies.log]
version = "0.4.8"
default-features = false

[dependencies.smallvec]
version = "1.6.1"
# M27 Phase 5c Stage G iter 5b: drop `union` feature — it requires
# `#![feature(untagged_unions)]` which is nightly-only. Stable smallvec
# uses the standard tagged enum representation, with a small perf cost.
features = []

[dependencies.target-lexicon]
version = "0.13.0"

[dev-dependencies.cranelift-codegen]
version = "0.122.0"
features = [
    "std",
    "unwind",
    "x86",
]
default-features = false

[dev-dependencies.env_logger]
version = "0.11.5"

[dev-dependencies.similar]
version = "2.1.0"

[lints.clippy]
allow_attributes_without_reason = "warn"
clone_on_copy = "warn"
from_over_into = "warn"
manual_strip = "warn"
map_clone = "warn"
redundant_field_names = "warn"
uninlined_format_args = "warn"
unnecessary_cast = "warn"
unnecessary_fallible_conversions = "warn"
unnecessary_mut_passed = "warn"
unnecessary_to_owned = "warn"
useless_conversion = "warn"

[lints.clippy.all]
level = "allow"
priority = -1

[lints.rust]
trivial_numeric_casts = "warn"
unstable_features = "warn"
unused-lifetimes = "warn"
unused-macro-rules = "warn"
unused_extern_crates = "warn"
unused_import_braces = "warn"

[lints.rust.unexpected_cfgs]
level = "warn"
priority = 0
check-cfg = [
    "cfg(pulley_tail_calls)",
    "cfg(pulley_assume_llvm_makes_tail_calls)",
    "cfg(pulley_disable_interp_simd)",
]
