[package]
name = "rustc_hir"
version = "0.0.0"
edition = "2024"

[dependencies]
# tidy-alphabetical-start
bitflags = "2.9.1"
rustc_abi = { path = "../rustc_abi" }
rustc_arena = { path = "../rustc_arena" }
rustc_ast = { path = "../rustc_ast" }
rustc_ast_pretty = { path = "../rustc_ast_pretty" }
rustc_data_structures = { path = "../rustc_data_structures" }
rustc_error_messages = { path = "../rustc_error_messages" }
rustc_hashes = { path = "../rustc_hashes" }
rustc_hir_id = { path = "../rustc_hir_id" }
rustc_index = { path = "../rustc_index" }
rustc_lint_defs = { path = "../rustc_lint_defs" }
rustc_macros = { path = "../rustc_macros" }
rustc_serialize = { path = "../rustc_serialize" }
rustc_span = { path = "../rustc_span" }
rustc_target = { path = "../rustc_target" }
smallvec = { version = "1.8.1", features = ["union", "may_dangle"] }
thin-vec = { version = "0.2.12", default-features = false }
tracing = { version = "0.1", default-features = false }
# tidy-alphabetical-end

# Phase 5b Stage E iter 5: odht pulls crc32fast + std I/O for the
# on-disk hash table format. We dropped incremental compilation per §1.3,
# so odht use sites are dead on SemOS target.
[target.'cfg(not(target_os = "none"))'.dependencies]
odht = { version = "0.3.1", features = ["nightly"] }

# M27 R4 B5: PathBuf in IntoDiagArg trait impls. Target-gated so host uses std
# PathBuf (via the `extern crate std as semos_std` alias in lib.rs) — matching
# rustc_error_messages' __IntoDiagArgPathBuf = std::path::PathBuf on host.
[target.'cfg(target_os = "none")'.dependencies]
semos-std = { path = "../../../../std-shim" }
