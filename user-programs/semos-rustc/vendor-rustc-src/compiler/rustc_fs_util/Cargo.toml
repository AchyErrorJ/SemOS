[package]
name = "rustc_fs_util"
version = "0.0.0"
edition = "2024"

[dependencies]
# tidy-alphabetical-start
# tidy-alphabetical-end

# Phase 5b Stage E iter 4: tempfile pulls once_cell (245 errors) transitively.
# Host-gate tempfile so the SemOS-target build doesn't pull it; the rustc_fs_util
# body's tempfile-using sites already have cfg(not(target_os = "none")) splits.
[target.'cfg(not(target_os = "none"))'.dependencies]
tempfile = "3.7.1"

[target.'cfg(target_os = "none")'.dependencies]
semos-std = { path = "../../../../std-shim" }
