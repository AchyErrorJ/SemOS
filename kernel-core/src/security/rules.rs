//! Policy Rules and Conditions
//!
//! Extended rule types and condition matching logic.

pub use super::policy::{PolicyRule, RuleCondition, RuleFlags};

/// Initialize rules subsystem
pub fn init() {
    // Rules subsystem initialized
}