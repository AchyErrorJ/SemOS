# `x86_64-runner` is a HOST tool that wraps the kernel ELF into bootable
# BIOS/UEFI disk images. Build it for the native host by default; pass an
# explicit `--target` only when cross-compiling the wrapper itself.
